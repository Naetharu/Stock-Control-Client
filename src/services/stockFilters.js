exports.getStockNumbers = (itemStatus, totalStock) => {
  let answer = 0;

  console.log("STOCK: ", totalStock);
  // for (const [key, value] of Object.entries(totalStock)) {
  //   if (value.status === itemStatus) {
  //     answer++;
  //     console.log("adding", value.status);
  //   }
  // }
  return answer;
};
